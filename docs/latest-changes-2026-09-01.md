# Последние изменения — 1 сентября 2026

Документ описывает изменения из коммитов `f421fc8`, `5d45970` и `783bb69`,
их пользовательское поведение, хранение данных, развёртывание и проверки.

## Публичный сайт

Верхняя часть лендинга перенесена из `freedom-vpn-mockup-v3.html`:

- белая навигация с переходами к возможностям, тарифам и кабинету;
- адаптивный тёмно-синий hero-блок;
- карточка-предпросмотр подключения;
- кнопки «Подключить» и «Выбрать подписку»;
- мобильная раскладка для ширины до 850 и 520 пикселей.

Кнопки подключения больше не выбирают первый тариф автоматически. Они открывают
модальное окно с тремя группами: «Лайт», «Стандарт» и «Ультра». Клик по
карточке группы или по конкретному сроку раскрывает остальные периоды этой
группы и выбирает тариф. В модальном окне нет email-поля: кнопка «Оплата»
переводит пользователя в `/cabinet?plan_id=...#payment`.

На лендинге в карточках тарифов остаются только активные публичные планы
длительностью 30 дней, а в модальном выборе доступны все активные публичные
сроки той же группы. Значения берутся из `plans`; HTML не содержит отдельной
копии цен. Выбранный `plan_id` передаётся в кабинет через query string и
повторно проверяется API при создании платежа.

## Разделение логотипов

Для сайта используется `api/app/static/freedom-vpn-logo-web.webp`: синий знак
на белом фоне, извлечённый из утверждённого макета. Он отображается в шапке,
карточке hero и web-кабинете.

Тёмный `bot/app/static/freedom-vpn-logo.png` не удалён и используется только
как фотография приветственного сообщения Telegram. Это предотвращает появление
тёмной квадратной подложки на белом сайте.

## Вход в web-кабинет

На `/cabinet` доступны два реально работающих режима:

1. «Код из письма» вызывает `POST /web/register`. API отправляет одноразовую
   шестизначную комбинацию, а в PostgreSQL хранит только её HMAC-SHA-256 с
   серверным секретом и привязкой к пользователю.
2. `POST /web/code/login` принимает email и код, допускает не более пяти
   попыток, одноразово погашает код и создаёт cookie-сессию.
3. «Пароль» вызывает `POST /web/password/login`. При успехе API создаёт новый
   `cabinet_access_tokens`, устанавливает `HttpOnly`, `SameSite=Strict` cookie
   и возвращает успешный JSON-ответ.

Переключатель меняет активную половину, поясняющий текст, поле пароля и основную
кнопку без перезагрузки страницы. «Забыли пароль?» запускает проверенный
email-сценарий и не раскрывает сохранённый пароль.

После подтверждения email-кода пароль создавать не обязательно: пользователь
сразу попадает в кабинет. При желании пароль по-прежнему можно сохранить через
`POST /web/password`, который требует действующую защищённую cookie.

Пароль никогда не сохраняется открытым текстом. `app.core.security` использует
PBKDF2-HMAC-SHA256, индивидуальную 16-байтовую соль и 600 000 итераций. Сравнение
хешей выполняется constant-time функцией `secrets.compare_digest`.

Миграция `b74e2c31a190` добавляет nullable-колонку `users.password_hash`. Поэтому
существующие пользователи продолжают входить по письму и могут создать пароль
после следующего подтверждённого входа. Миграция `c91a08f4d211` добавляет
таблицу короткоживущих `cabinet_login_codes`; откат этой миграции возвращает
старый backend без удаления пользователей и cookie-сессий.

### Публичные маршруты аутентификации

| Метод | Маршрут | Назначение |
|---|---|---|
| `POST` | `/web/register` | Создать/найти пользователя и отправить email-код |
| `POST` | `/web/code/login` | Проверить и погасить код, установить cookie |
| `GET` | `/cabinet/access/{token}` | Совместимость с ранее выданной ссылкой |
| `POST` | `/web/password/login` | Проверить email и пароль, создать сессию |
| `GET` | `/cabinet/password` | Показать настройку пароля авторизованному пользователю |
| `POST` | `/web/password` | Сохранить новый пароль авторизованному пользователю |
| `POST` | `/cabinet/logout` | Удалить cookie кабинета |

Страницы кабинета отправляют `Cache-Control: no-store`, CSP, запрет frame и
`Referrer-Policy: no-referrer`. При HTTPS cookie дополнительно получает
`Secure`. Ошибка парольного входа не уточняет, существует ли email.

## Покупка и кабинет

На лендинге и в кабинете планы сгруппированы по верхнему уровню тарифа. Клик
по карточке группы раскрывает скрытые сроки и выбирает первый доступный план,
клик по сроку выбирает конкретный `plan_id`. Выбор страны из web-интерфейса
удалён: API использует активную ноду текущего клиента, а при её отсутствии —
первую активную ноду. Внешние способы оплаты открывают настроенную платёжную
ссылку, ручные способы создают платёж в VPN API и показывают QR/инструкцию с
загрузкой чека.

Одна подписка может иметь несколько исторических платежей. Ограничение unique
на `payments.subscription_id` заменено обычным индексом миграцией
`a6d9274f1c03`. При сбое фиксации продления компенсация восстанавливает прежнего
клиента 3x-ui, а не оставляет пользователя без доступа.

## Telegram-бот

Inline-меню и постоянная Reply-клавиатура используют две кнопки в строке:

- покупка / управление подпиской;
- промокод / пробный доступ;
- инструкции / поддержка;
- канал / сайт.

Под приветственным постом «Сайт» и «Email» также находятся в одной строке.
Callback data и обработчики не менялись. Тёмный Telegram-логотип не затронут.

## Почта, публикация и сертификаты

Публичный адрес текущего стенда — `https://freedomvpn.taile485ac.ts.net`. Tailscale
Funnel проксирует его на `127.0.0.1:8000`; API не открывает порт напрямую во
внешнюю сеть. Автоподъём Funnel оформлен unit-файлом
`deploy/systemd/vpn-tailscale-funnel.service`. Postfix слушает только loopback
и Docker bridge и используется API через `host.docker.internal:25`.

`scripts/renew_tailscale_cert.sh` атомарно обновляет файловый сертификат для
внешних listeners. Таймер `vpn-tailscale-cert.timer` запускает проверку
еженедельно. Сам Funnel получает и продлевает сертификат автоматически.

## Развёртывание

Обязательный порядок для версии `783bb69` и новее:

```bash
git pull --ff-only origin newnode
docker compose build api bot
docker compose run --rm api alembic upgrade head
docker compose up -d --force-recreate api bot
```

Проверки после запуска:

```bash
docker compose run --rm api alembic current
curl -fsS http://127.0.0.1:8000/health
curl -fsS https://freedomvpn.taile485ac.ts.net/
docker compose ps api bot worker
docker compose logs --tail=100 api bot worker
tailscale funnel status
systemctl is-active postfix vpn-tailscale-funnel.service vpn-tailscale-cert.timer
```

Ожидаемая миграция: `b74e2c31a190 (head)`. `/cabinet` без cookie намеренно
возвращает `401` вместе с HTML-формой входа — это не ошибка health-check.

## Проверки разработки

Перед коммитом выполнены:

```bash
.venv/bin/python -m compileall -q api/app bot/app
.venv/bin/alembic heads
.venv/bin/python -m unittest discover -s tests -p 'test_*.py'
git diff --check
```

Сценарий web-аутентификации проверяет хешированное хранение email-кода, отказ
при неверной комбинации, одноразовость, cookie-сессию и сохранённый парольный
вход. Лендинг и оба состояния переключателя проверяются вместе с API.
